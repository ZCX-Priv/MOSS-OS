{
  "name": "copy",
  "description": "复制文件/目录（替代 shell 的 cp/Copy-Item）。支持批量（sources 数组）。目录递归复制（100MB 上限）。覆盖策略：error（默认，目标存在即失败）/ rename（自动重命名避开冲突）/ overwrite（覆盖目标，改前自动备份支持 undo）。复制产生的副本登记为 create 条目（undo = 删除副本）。",
  "icon": "copy",
  "annotations": {
    "idempotentHint": false
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "source": {
        "type": "string",
        "description": "源文件/目录的绝对或相对路径（单源模式，与 sources 互斥）"
      },
      "sources": {
        "type": "array",
        "items": { "type": "string" },
        "description": "批量源路径数组（多源模式，与 source 互斥；此时 dest 必须是已存在的目录）"
      },
      "dest": {
        "type": "string",
        "description": "目标路径。单源时可为新路径或已存在目录（复制入内）；多源时必须是已存在目录"
      },
      "overwrite": {
        "type": "string",
        "enum": ["error", "rename", "overwrite"],
        "default": "error",
        "description": "目标已存在时的策略：error=报错（默认）；rename=自动改名；overwrite=覆盖（覆盖前备份，支持 undo）"
      },
      "dryRun": {
        "type": "boolean",
        "default": false,
        "description": "预演模式：返回将执行的复制清单，不执行任何操作"
      }
    },
    "required": ["dest"],
    "additionalProperties": false
  },
  "config": {
    "defaults": {
      "enabled": true,
      "trackHistory": true
    },
    "schema": {
      "trackHistory": { "type": "boolean" }
    }
  }
}
