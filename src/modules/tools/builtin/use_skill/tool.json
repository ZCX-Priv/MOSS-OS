{
  "name": "use_skill",
  "description": "调用已注册的技能。技能是从 skills/ 目录加载的提示词模板。返回技能提示词文本（通过 args 替换 {{placeholder}}）。可用内置技能：brainstorming、code-review、tdd、explain。",
  "icon": "sparkles",
  "annotations": {
    "readOnlyHint": true,
    "idempotentHint": true
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "skill": {
        "type": "string",
        "description": "要调用的技能名称"
      },
      "args": {
        "type": "object",
        "description": "替换到技能提示词占位符中的参数（如 {topic: \"react hooks\"} 对应 {{topic}}）。",
        "additionalProperties": true
      }
    },
    "required": ["skill"],
    "additionalProperties": false
  },
  "config": {
    "defaults": { "enabled": true },
    "schema": {}
  }
}
