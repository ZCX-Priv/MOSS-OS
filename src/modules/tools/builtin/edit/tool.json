{
  "name": "edit",
  "description": "通过将特定字符串替换为新字符串来编辑文件。除非 replaceAll 为 true，否则 oldString 必须在文件中唯一。使用 replaceAll 替换所有出现位置。",
  "icon": "file-pen",
  "annotations": {
    "destructiveHint": true
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "要编辑的文件的绝对或相对路径"
      },
      "oldString": {
        "type": "string",
        "description": "要在文件中查找的精确字符串"
      },
      "newString": {
        "type": "string",
        "description": "用于替换 oldString 的字符串"
      },
      "replaceAll": {
        "type": "boolean",
        "description": "如果为 true，替换所有出现的 oldString（默认 false）"
      }
    },
    "required": ["path", "oldString", "newString"],
    "additionalProperties": false
  },
  "config": {
    "defaults": { "enabled": true, "requireConfirmation": false },
    "schema": {}
  }
}
