{
  "name": "write",
  "description": "Write content to a file, overwriting if it exists. Parent directories are created automatically if createDirs is true. This is a destructive operation.",
  "icon": "file-plus",
  "annotations": {
    "destructiveHint": true,
    "requireConfirmation": true
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "Absolute or relative path to the file to write."
      },
      "content": {
        "type": "string",
        "description": "The content to write to the file."
      },
      "createDirs": {
        "type": "boolean",
        "description": "Whether to create parent directories if they do not exist (default true)."
      }
    },
    "required": ["path", "content"],
    "additionalProperties": false
  },
  "config": {
    "defaults": { "enabled": true, "requireConfirmation": true },
    "schema": {}
  }
}
