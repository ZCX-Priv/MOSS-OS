{
  "name": "get_spec",
  "description": "按 id 读取规范文档的完整内容。spec id 是 agent/prompts/main/spec/ 下的相对路径（不含 .md 扩展名，如 \"coding/typescript\"、\"safety\"）。先使用 list_spec 发现可用的 spec id。返回规范的 Markdown 正文（去除 front-matter）。",
  "icon": "file-code",
  "annotations": {
    "readOnlyHint": true,
    "idempotentHint": true
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "spec": {
        "type": "string",
        "description": "要读取的 spec id（相对路径不含 .md，如 \"coding/typescript\"）"
      }
    },
    "required": ["spec"],
    "additionalProperties": false
  },
  "config": {
    "defaults": { "enabled": true },
    "schema": {}
  }
}
