{
  "name": "delete",
  "description": "删除文件或目录。默认送回收站（trash=true，可恢复7天），force=true 且 trash=false 时永久硬删除（删除前自动备份：文件用哈希内容寻址备份，目录用 tar.gz 整体归档，均支持 undo 恢复）。支持批量（paths 数组）与预演模式（dryRun）。安全校验：路径越权、symlink 遍历、Windows 路径折叠、dev vault（.git）防护、read-before-delete。此操作具有破坏性。",
  "icon": "trash-2",
  "annotations": {
    "destructiveHint": true,
    "requireConfirmation": true
  },
  "inputSchema": {
    "type": "object",
    "properties": {
      "path": {
        "type": "string",
        "description": "要删除的文件或目录的绝对或相对路径（单路径模式，与 paths 互斥）"
      },
      "paths": {
        "type": "array",
        "items": { "type": "string" },
        "description": "批量删除的路径数组（多路径模式，与 path 互斥）"
      },
      "recursive": {
        "type": "boolean",
        "description": "删除目录时必填。递归删除目录树。对文件忽略"
      },
      "trash": {
        "type": "boolean",
        "default": true,
        "description": "默认 true 送回收站（可恢复7天）。设为 false 且 force=true 时永久硬删除"
      },
      "force": {
        "type": "boolean",
        "default": false,
        "description": "强制模式：跳过 read-before-delete 与 dev vault 防护，rmSync 用 force 覆盖只读文件。仅与 trash=false 配合生效"
      },
      "dryRun": {
        "type": "boolean",
        "default": false,
        "description": "预演模式：返回将被删除的路径清单与总大小，不执行任何删除操作"
      }
    },
    "additionalProperties": false
  },
  "config": {
    "defaults": {
      "enabled": true,
      "requireConfirmation": true,
      "trackHistory": true
    },
    "schema": {
      "trackHistory": { "type": "boolean" }
    }
  }
}
